// import React from 'react';
// import { Card, Select } from 'antd';
// import { Line, Pie } from '@ant-design/plots';

// const { Option } = Select;

// interface ChartCardProps {
//   title: string;
//   chartType: 'line' | 'pie';
//   data: any;
//   config: any;
//   extraOptions: React.ReactNode;
// }

// const ChartCard: React.FC<ChartCardProps> = ({ title, chartType,  config, extraOptions }) => {
//   return (
//     <Card title={title} extra={extraOptions}>
//       {chartType === 'line' && <Line {...config} height={300} />}
//       {chartType === 'pie' && <Pie {...config} height={300} />}
//     </Card>
//   );
// };

// export default ChartCard;
